:root{
  --c-primary:#123B6B;
  --c-primary-700:#0E2F55;
  --c-secondary:#2491D9;
  --c-accent:#FF8A27;
  --c-bg:#0A0F17;
  --c-text:#FFFFFF;
}

*{box-sizing:border-box}
html,body{margin:0;padding:0;background:var(--c-bg);color:var(--c-text);font-family:Inter,system-ui,Segoe UI,Roboto,Arial,sans-serif}
img{max-width:100%;display:block}
a{color:inherit;text-decoration:none}

.container{width:min(1120px,92%);margin-inline:auto}
.section{padding:64px 0}
.section.alt{background:rgba(255,255,255,0.04)}
h1,h2,h3{font-family:Poppins,Inter,system-ui,sans-serif;margin:0 0 12px}
p{margin:0 0 10px;color:#E8EEF6}

.site-header{position:sticky;top:0;background:rgba(10,15,23,0.65);backdrop-filter:blur(8px);z-index:10;border-bottom:1px solid rgba(255,255,255,0.06)}
.header-inner{display:flex;align-items:center;justify-content:space-between;padding:10px 0}
.brand{display:flex;gap:10px;align-items:center;font-weight:700}
.brand img{width:40px;height:40px;object-fit:contain}

.nav{display:flex;gap:14px;align-items:center}
.btn{display:inline-flex;align-items:center;justify-content:center;padding:10px 16px;border-radius:10px;border:1px solid transparent;font-weight:600;cursor:pointer;transition:.2s}
.btn-primary{background:linear-gradient(135deg,var(--c-primary) 0%, var(--c-secondary) 100%);border-color:transparent}
.btn-ghost{border-color:rgba(255,255,255,.15);background:transparent}
.btn:hover{transform:translateY(-1px)}

.hero{position:relative;padding:90px 0 40px}
.hero-inner{display:grid;grid-template-columns:1.1fr .9fr;gap:28px;align-items:center}
.glass{background:rgba(255,255,255,0.06);border:1px solid rgba(255,255,255,0.12);border-radius:16px;padding:22px;box-shadow:0 10px 30px rgba(0,0,0,0.25)}
.bullets{padding:0;margin:10px 0 14px;list-style:none;display:grid;gap:6px}
.bullets li::before{content:"• ";color:var(--c-accent);font-weight:900;margin-right:2px}
.badges{display:flex;flex-wrap:wrap;gap:8px;margin-top:8px}
.badge{background:rgba(36,145,217,.2);color:#CFE9FF;border:1px solid rgba(36,145,217,.35);padding:6px 10px;border-radius:999px;font-size:.85rem}

.grid{display:grid;gap:16px}
.services-grid{grid-template-columns:repeat(auto-fill,minmax(220px,1fr))}
.features-grid{grid-template-columns:repeat(auto-fill,minmax(240px,1fr))}
.card{background:rgba(255,255,255,0.05);border:1px solid rgba(255,255,255,0.1);border-radius:14px;padding:16px}
.card h3{margin-bottom:6px}

.form{margin-top:16px}
.form-grid{grid-template-columns:repeat(3,1fr)}
.form-grid label{display:flex;flex-direction:column;gap:6px}
.form-grid .full{grid-column:1/-1}
input,select,textarea{background:#0F1522;border:1px solid #1E293B;border-radius:10px;color:#E6F0FF;padding:10px}
input:focus,select:focus,textarea:focus{outline:none;border-color:var(--c-secondary);box-shadow:0 0 0 3px rgba(36,145,217,.25)}
.privacy{opacity:.75;font-size:.9rem;margin-top:10px}

.masonry{columns:2;column-gap:14px}
.gallery-img{width:100%;margin:0 0 14px;border-radius:12px;border:1px solid rgba(255,255,255,0.1);cursor:pointer;transition:transform .2s}
.gallery-img:hover{transform:scale(1.01)}

.video-grid{grid-template-columns:repeat(auto-fill,minmax(320px,1fr))}
.video iframe{width:100%;aspect-ratio:16/9;border-radius:12px;border:1px solid rgba(255,255,255,0.1)}

.accordion details{background:rgba(255,255,255,.05);border:1px solid rgba(255,255,255,.1);border-radius:12px;padding:10px 14px}
.accordion details+details{margin-top:10px}
.accordion summary{cursor:pointer;font-weight:600;color:#DCE8F8}

.contact{display:grid;grid-template-columns:1fr 1fr;gap:24px}
.contact-data p{margin:4px 0;color:#D3E4F5}

.site-footer{border-top:1px solid rgba(255,255,255,0.06);padding:22px 0;color:#C8D6EA}
.footer-inner{display:flex;justify-content:center}

.whatsapp-fab{
  position:fixed;bottom:25px;right:25px;background:#25D366;width:60px;height:60px;border-radius:50%;
  box-shadow:0 4px 10px rgba(0,0,0,.3);display:flex;align-items:center;justify-content:center;z-index:9999;transition:transform .2s
}
.whatsapp-fab:hover{transform:scale(1.08)}

.modal{border:none;border-radius:14px;padding:0;background:transparent}
.modal::backdrop{background:rgba(0,0,0,.5);backdrop-filter:blur(4px)}
.modal-content{transform-style:preserve-3d;border-radius:14px;overflow:hidden}
.modal-content img{max-width:80vw;max-height:75vh;display:block}
.modal-close{position:absolute;top:8px;right:12px;background:rgba(0,0,0,.6);color:#fff;border:none;border-radius:8px;padding:6px 10px;cursor:pointer}

#bg-canvas{position:fixed;inset:0;width:100%;height:100%;z-index:-1;opacity:.6;pointer-events:none}

@media (max-width: 900px){
  .hero-inner{grid-template-columns:1fr}
  .form-grid{grid-template-columns:1fr}
  .masonry{columns:1}
  .contact{grid-template-columns:1fr}
}
